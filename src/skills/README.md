# CockroachDB Agent Skills Used

We import from https://github.com/cockroachlabs/agent-skills

- onboarding: cluster provisioning via ccloud CLI JSON output
- query-optimization: vector index tuning (lists=100, HNSW)
- observability: memory_events audit for agent decisions
- security: read-only MCP server + service account RBAC
